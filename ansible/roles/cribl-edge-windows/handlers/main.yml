# =============================================================================
# Handlers — cribl-edge-windows
# =============================================================================

- name: restart cribl-edge-windows
  ansible.windows.win_service:
    name: "{{ service_name }}"
    state: restarted
